# 📱 Улучшенная система уведомлений о лекарствах

## 🎯 Обзор улучшений

Новая система уведомлений решает все критические проблемы доставки и значительно повышает надежность:

### ✅ Решенные проблемы

1. **Android 13+ поддержка** - Добавлено разрешение `POST_NOTIFICATIONS`
2. **Восстановление после перезапуска** - Уведомления автоматически восстанавливаются
3. **Управление уведомлениями** - Полная поддержка отмены и обновления
4. **Действия в уведомлениях** - Пользователь может ответить прямо из уведомления
5. **Обработка ошибок** - Пользователь видит понятные сообщения об ошибках
6. **Проверка разрешений** - Приложение проверяет статус разрешений

---

## 🏗️ Архитектура

### Основные компоненты

```
src/
├── services/
│   └── NotificationManager.ts    # Центральный менеджер уведомлений (Singleton)
├── hooks/
│   └── useReminders.ts           # Обновленный hook с новой системой
└── utils/
    └── notifications.ts          # УСТАРЕВШИЙ - больше не используется
```

### NotificationManager (Singleton)

Центральный сервис для управления уведомлениями:

```typescript
import NotificationManager from '../services/NotificationManager';

// Инициализация (автоматически при первом использовании)
await NotificationManager.initialize();

// Проверка разрешений
const { granted } = await NotificationManager.checkPermission();

// Планирование уведомления
const notificationId = await NotificationManager.scheduleNotification({
  reminderId: 'reminder-123',
  title: 'Напоминание: Аспирин',
  body: 'Примите 1 таблетку',
  date: new Date(Date.now() + 3600000), // через 1 час
});

// Отмена уведомления
await NotificationManager.cancelNotification('reminder-123');

// Отмена нескольких уведомлений
await NotificationManager.cancelNotifications(['reminder-1', 'reminder-2']);

// Восстановление после перезапуска
await NotificationManager.restoreNotifications(reminders);

// Очистка старых уведомлений
await NotificationManager.cleanup();
```

---

## 📋 Использование в компонентах

### Получение доступа к системе уведомлений

```typescript
import { useReminders } from '../hooks/useReminders';

function MyComponent() {
  const {
    reminders,
    loading,
    notificationPermission,
    scheduleReminders,
    updateReminderStatus,
    deleteReminder,
    requestNotificationPermission,
  } = useReminders();

  // Проверка разрешений
  useEffect(() => {
    if (notificationPermission === false) {
      Alert.alert(
        'Разрешение на уведомления',
        'Пожалуйста, включите уведомления в настройках для получения напоминаний о лекарствах.',
        [
          { text: 'Отмена', style: 'cancel' },
          {
            text: 'Настройки',
            onPress: () => requestNotificationPermission()
          }
        ]
      );
    }
  }, [notificationPermission]);

  // Создание напоминания
  const handleAddReminder = async () => {
    const newReminders = [
      {
        id: 'unique-id',
        name: 'Аспирин',
        dosage: '1 таблетка',
        time: '09:00',
        date: '2025-11-07',
        status: 'pending',
        type: 'tablet',
      }
    ];

    const result = await scheduleReminders(newReminders);

    if (!result.success) {
      Alert.alert('Ошибка', result.error);
    }
  };

  return (
    <View>
      {/* UI компонента */}
    </View>
  );
}
```

---

## 🔔 Действия в уведомлениях

Пользователи могут взаимодействовать с уведомлениями без открытия приложения:

### Доступные действия

1. **"Принял"** - Отмечает лекарство как принятое
2. **"Отложить (15 мин)"** - Напоминание придет через 15 минут
3. **"Пропустить"** - Отмечает лекарство как пропущенное

### Автоматическая обработка

NotificationManager автоматически обрабатывает все действия:
- Обновляет статус в базе данных
- Отменяет или перепланирует уведомления
- Логирует все действия

---

## ⚙️ Разрешения Android

### Добавленные разрешения в AndroidManifest.xml

```xml
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.VIBRATE" />
<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
```

### Назначение разрешений

- **POST_NOTIFICATIONS** - Обязательно для Android 13+ (API 33+)
- **VIBRATE** - Вибрация при уведомлении
- **USE_FULL_SCREEN_INTENT** - Полноэкранные уведомления для критических напоминаний
- **SCHEDULE_EXACT_ALARM** - Точное время доставки уведомлений
- **RECEIVE_BOOT_COMPLETED** - Восстановление уведомлений после перезагрузки устройства

---

## 🔄 Восстановление после перезапуска

Система автоматически восстанавливает уведомления:

1. **При загрузке приложения** - `useReminders` вызывает `restoreNotifications()`
2. **Проверка актуальности** - Пропускает уведомления из прошлого
3. **Избегание дубликатов** - Проверяет существующие уведомления перед планированием
4. **Очистка** - Удаляет устаревшие записи из хранилища

### Внутренний механизм

```typescript
// В useReminders.ts
const fetchReminders = useCallback(async () => {
  const stored = await AsyncStorage.getItem('reminders');
  if (stored) {
    const loadedReminders = JSON.parse(stored);
    setReminders(loadedReminders);

    // ✅ Автоматическое восстановление
    await NotificationManager.restoreNotifications(loadedReminders);

    // ✅ Очистка старых
    await NotificationManager.cleanup();
  }
}, []);
```

---

## 📊 Хранение данных

### AsyncStorage структура

#### Напоминания (`reminders`)
```json
[
  {
    "id": "reminder-123",
    "name": "Аспирин",
    "dosage": "1 таблетка",
    "time": "09:00",
    "date": "2025-11-07",
    "status": "pending",
    "type": "tablet",
    "courseId": 1
  }
]
```

#### Запланированные уведомления (`scheduled_notifications`)
```json
[
  {
    "reminderId": "reminder-123",
    "notificationId": "notif-456",
    "scheduledTime": 1699344000000
  }
]
```

---

## 🐛 Отладка

### Проверка статуса уведомлений

```typescript
// Получить все активные уведомления
const active = await NotificationManager.getActiveNotifications();
console.log('Active notifications:', active);

// Проверить разрешения
const { granted, status } = await NotificationManager.checkPermission();
console.log('Permission:', granted ? 'GRANTED' : 'DENIED', status);
```

### Логирование

NotificationManager логирует все операции:
- ✅ Успешное планирование
- ❌ Ошибки планирования
- 🔄 Восстановление уведомлений
- 🗑️ Отмена уведомлений
- 🧹 Очистка старых записей

---

## 🚀 Миграция со старой системы

### Шаги миграции

1. **Обновите импорты** - Используйте `NotificationManager` вместо `reminderNotification`
2. **Обработайте ответы** - `scheduleReminders()` теперь возвращает `{ success, error? }`
3. **Проверьте разрешения** - Используйте `notificationPermission` из хука
4. **Удалите старый код** - Файл `utils/notifications.ts` устарел

### Пример миграции

**До:**
```typescript
import { reminderNotification } from '../utils/notifications';

reminderNotification({
  title: 'Напоминание',
  body: 'Примите лекарство',
  date: new Date(),
});
```

**После:**
```typescript
const { scheduleReminders } = useReminders();

const result = await scheduleReminders([reminder]);
if (!result.success) {
  Alert.alert('Ошибка', result.error);
}
```

---

## 📈 Преимущества новой системы

| Функция | Старая система | Новая система |
|---------|---------------|---------------|
| Android 13+ | ❌ Не работает | ✅ Полная поддержка |
| Восстановление | ❌ Теряются при перезапуске | ✅ Автоматическое восстановление |
| Управление | ❌ Нет отмены уведомлений | ✅ Полное управление |
| Действия | ❌ Только открытие приложения | ✅ Take/Snooze/Skip |
| Обработка ошибок | ❌ Только console.warn | ✅ Понятные сообщения пользователю |
| Проверка разрешений | ❌ Нет проверки | ✅ Проактивная проверка |
| Очистка | ❌ Устаревшие уведомления накапливаются | ✅ Автоматическая очистка |

---

## 🔮 Дальнейшие улучшения

Возможные дополнительные функции:

1. **Повторяющиеся уведомления** - Еженедельные/ежемесячные напоминания
2. **Кастомные звуки** - Разные звуки для разных лекарств
3. **Геолокация** - Напоминания при входе/выходе из определенной локации
4. **Умные напоминания** - Адаптация времени на основе поведения пользователя
5. **Статистика доставки** - Отслеживание успешности доставки уведомлений
6. **Синхронизация с облаком** - Восстановление на разных устройствах

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи в консоли
2. Убедитесь, что разрешения предоставлены
3. Проверьте, что устройство не в режиме энергосбережения
4. Для Android 13+ убедитесь, что targetSdkVersion >= 33

---

**Версия документа:** 1.0
**Последнее обновление:** 2025-11-06
**Автор:** Claude Code Assistant
